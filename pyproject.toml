[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "audittrail"
version = "0.1.0"
description = "Defensive audit event processing helpers"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["audittrail"]
